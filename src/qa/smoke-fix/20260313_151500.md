● Fixed the /tournament crash from your smoke report.

  I updated utils/database/dbMatches.js:

   - getNumberOfOwnGoalsInTournament: replaced the invalid subquery-in-FROM with a direct JOIN + COALESCE(SUM(...), 0) and return numeric value.
   - getNumberOfCardsInTournament: fixed the same SQL pattern on that same endpoint path (prevents the next crash after own-goals query).

  Validation: node --check utils/database/dbMatches.js passed, and running both functions against DB returned {"tournamentId":7,"ownGoals":0,"totalCards":1}
  with no PostgreSQL syntax errors. (npm test is a placeholder script here.) Please rerun qa/smoke/20260313_151500.md to confirm end-to-end.
